{
  "scripts": {
    "start": "cross-env NODE_ENV=production node server.js",
    "build": "mkdir -p public && echo 'hello from index.txt' > public/index2.txt"
  },
  "dependencies": {
    "@vercel/node": "^1.9.1",
    "@types/lowdb": "^1.0.9",
    "chalk": "^4.1.0",
    "http-proxy-agent": "^4.0.1",
    "json-server": "^0.16.3",
    "lowdb": "^1.0.0",
    "node-fetch": "^2.6.1"
  },
  "devDependencies": {
    "prettier": "^2.2.1"
  }
}
